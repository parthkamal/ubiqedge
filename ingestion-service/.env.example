# copy to .env for local development — never commit the real .env

PORT=3001

# MySQL — same instance/schema the backend connects to
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=root
DB_NAME=ubiqedge_water_billing
