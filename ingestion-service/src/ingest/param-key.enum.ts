// mirrors backend's ParamKey (device/entities/device-type-param.entity.ts) —
// duplicated on purpose, see ingest.module.ts
export enum ParamKey {
  LEVEL = 'LEVEL',
  TOTAL = 'TOTAL',
  FLOW = 'FLOW',
}
