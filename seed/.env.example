# copy to .env for local development — never commit the real .env
# same DB the backend/ingestion-service connect to

DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=root
DB_NAME=ubiqedge_water_billing
